<?php
/**
 * @file
 * Module file for Rest API.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function myrest_api_ctools_plugin_directory($module, $plugin) {
  if ($module == 'restful') {
    return 'plugins/' . $plugin;
  }
}